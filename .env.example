# Google Analytics 4 Measurement ID (looks like G-XXXXXXXXXX).
# Get it from https://analytics.google.com → Admin → Data Streams → your web stream.
# Copy this file to `.env` and fill in your real ID. Analytics only runs in
# production builds (`npm run build` / `npm run preview`), not in `npm run dev`.
VITE_GA_MEASUREMENT_ID=
